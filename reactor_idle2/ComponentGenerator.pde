class ComponentGenerator extends Component {
  int rank;
  float[] prices = {1};
  float[] generate_amount = {0.1};
  ComponentGenerator(int x, int y, int rank) {
    super(x, y);
    this.x = x;
    this.y = y;
    this.rank = rank;
  }
  
  
  void tick() {
    
    
  }
}
